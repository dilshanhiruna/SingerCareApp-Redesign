<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uee.singercare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SingerCareApp">
        <activity
            android:name="Hashen.SelectLocation"
            android:exported="true" />
        <activity
            android:name="Hashen.CreateRequest"
            android:exported="true"></activity>
        <activity
            android:name="Hashen.SendInquiry"
            android:exported="true">

        </activity>
        <activity android:name="Pabasara.forgot_password" />
        <activity android:name="Pabasara.signup" />
        <activity android:name="Pabasara.login" />
        <activity android:name="Pabasara.selecet_language" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Dilshan.RateService" />
        <activity android:name="Dilshan.MyHistory" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>